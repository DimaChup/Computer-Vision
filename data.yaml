# filename: data.yaml
# YOLOv8 Dataset Configuration
# This assumes the 'dataset' folder is in the same directory as this file.

path: .                 # Root directory (relative to where training runs)
train: dataset/images   # Training images (relative to 'path')
val: dataset/images     # Validation images (using same for simple test)

# Classes
nc: 1           # Number of classes
names:
  0: dummy      # Class name